#!/bin/sh
set -eu

if [ "${3:-/}" != "/" ]; then
    echo "vex-agent can only be installed on the active system volume" >&2
    exit 1
fi

launchctl bootout system/io.jimber.vex-agent 2>/dev/null || true

exit 0
